custom:
  message: Centralized config for Customer Service
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankingcustomerdb
    username: root
    password: admin
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl:
        auto: update

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/
