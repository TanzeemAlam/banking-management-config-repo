server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankingaccountdb
    username: root
    password: admin
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl:
        auto: update

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/
